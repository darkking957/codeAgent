# CoreAgent

终端 AI 编程助手（类似 Claude Code），命令行交互，Python 实现。

> 本文件每次会话开头都进上下文。**保持精简**：每行问「删了它 Claude 会不会犯错」，不会就删。

## 技术栈

- **LangGraph** —— Agent 编排与状态机
- **FastAPI** —— 服务层 / 本地 API（如需）
- **RAG** —— 代码库检索
- **MCP** —— 工具与外部上下文接入

技术栈是背景；具体设计由 spec 定。

注意：代码实现要工程化。
## 目录结构

```
CLAUDE.md            本文件
specs/
  0001-<slug>/       一个想法 / 任务一个文件夹
    spec.md
    tasks.md
    checklist.md
  _done/             已完成挪这里
```

## 工作循环

四步，**不许跳步**。一句话能说清的小改才直接动手。

1. **面试我** —— 追问，把不确定问清：不做什么、异常路径、与现有能力冲突、隐含假设。一轮 3~6 个有取舍的问题，按重要性排序。不替我拍板，二选一摆出代价让我选。问到三份文档能不靠猜写出来 → 停。
2. **写文档** —— 到 `specs/NNNN-<slug>/` 写 `spec.md` `tasks.md` `checklist.md`（中文）。
3. **新会话实现** —— 文档定稿后开新会话，只加载该文件夹（+ 依赖的 spec）；照 tasks 实现，对 checklist 逐条跑、贴证据，别口头说「完成」。
4. **评审** —— 用干净上下文对着 spec + checklist 复查 diff：需求是否落地、边缘场景是否有验证、有没有动到范围外。只报影响正确性 / 需求的缺口，不报风格。

写文档时自检对齐：

- spec 每条能力 → tasks 至少一个任务覆盖。
- spec 砍掉的具体值 → checklist 的验收项。
- tasks 每个任务 → checklist 有可观测验收。

## 全局铁律

- **最简方案优先**：能不加抽象、不引依赖就不加。
- **改根因，不改症状**。
- 不可逆操作（删文件、改库、批量重命名）前先 commit / 备份。
- 一个会话一件事。无关任务、或同一问题纠正两次还不对 → 清空上下文、用更好的 prompt 重来。
- 改动全局约定（架构、技术栈）才回头改本文件。

## spec.md —— 做什么，什么算完成

顶部一行状态：草稿 / 进行中 / 已完成。

写：背景、目标用户、能力清单（一句一条）、非功能要求、设计骨架、Out of Scope。

**不写**：函数名 / 参数名 / 默认值 / 错误文本 / 行号 / SDK 类型名——会过期，归 checklist。

## tasks.md —— 什么顺序，动哪些文件

- 5~15 个任务，每个一次会话内做完。
- 每个标注：**影响文件**、**依赖任务**、**参考定位**（精确到函数 / 行号）。
- 能独立验证的靠前；有依赖的排在被依赖项之后。
- 末尾两个雷打不动：「接入主流程」+「端到端验证」。

> **T3｜实现检索节点**
> 影响文件：`src/agent/nodes/retrieve.py`
> 依赖：T1（RAG 索引就绪）
> 参考：`StateGraph.add_node`

## checklist.md —— 可运行，可观测

每条能跑出 pass / fail，不靠人脑判断。禁止「实现完整」「质量良好」「运行正常」。跑完贴证据（命令 + 输出），别只打勾。

- spec 砍掉的具体值（错误文本、默认值、阈值）落这里。
- [ ] `grep -r "X" src/` 返回 ≥3 条
- [ ] 输入 Y，终端输出含 Z
- [ ] 缺参数报错文案精确为「<填>」
- [ ] 默认 timeout = <填> 秒
- 至少一条端到端验收。

## 增量与归档

增量分三种：

- **新能力** → 开新文件夹 `specs/NNNN-<slug>/`。
- **小修**（bug / 微调）→ 在原文件夹做：状态改回「进行中」，补 checklist 项，重验，再归档。别为一行修复开新号。
- **大改 / 改契约** → 开新文件夹，spec 顶部注明 `supersedes #NNNN`，旧 spec 加一行指向新号。不改已完成文件夹正文，过期就显式作废。

规矩：

- 跨功能引用按 **ID**（`#0003`），不按路径，归档才不断链。（文件夹内任务仍用 `T1`、`T2`。）
- 下一个编号 = `specs/` 与 `specs/_done/` 中最大号 + 1。
- 归档触发 = checklist 全绿且贴了证据（≠ 任务勾完）；spec 标「已完成」，文件夹挪进 `specs/_done/`。
- 一个文件夹别无限膨胀：超出原 spec 范围就另开新号。