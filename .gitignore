# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*$py.class

# 分发 / 打包
*.egg
*.egg-info/
dist/
build/
.eggs/
wheels/
*.whl
MANIFEST
pip-wheel-metadata/

# 安装器日志
pip-log.txt
pip-delete-this-directory.txt

# ── 虚拟环境 ─────────────────────────────────────────────
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# ── 类型检查 / 测试 ───────────────────────────────────────
.mypy_cache/
.dmypy.json
dmypy.json
.pytype/
.pyre/

.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
nosetests.xml
*.cover

# ── 工具链 ───────────────────────────────────────────────
.ruff_cache/
.tox/
.nox/

# ── Jupyter ──────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── 环境变量 / 密钥（绝不入库）──────────────────────────────
.env
.env.*
!.env.example
config.yaml
!config.yaml.example
*.pem
*.key
*.p12
secrets/

# 注：docker-compose.prod.yml 与 Caddyfile 不含明文机密（密钥走 .env），随仓库入库。
# Caddyfile 仅含域名 + 门口令的 bcrypt 哈希（单向）；仓库若公开可考虑把哈希也外置。

# ── 部署仓库：不纳入内部规格 / 测试 / e2e 脚本 ──────────────────
# specs 与 tests 仅本地保留（不再随仓库分发，故也不再被版本管理）；保留 scripts/build_frontend.sh（部署用）。
specs/
tests/
scripts/e2e_teams.py
scripts/web_e2e_*.py

# ── CoreAgent 本地配置（#0007 权限系统）──────────────────────
# Local 作用域为本地临时覆盖，不进 Git（含 persist 落盘的 allow 规则）。
.coreagent/settings.local.json

# ── CoreAgent 本地个人级指令（#0010 记忆系统）────────────────
# 本地个人级项目指令文件不入库（仅本机生效）。
COREAGENT.local.md

# ── 数据库 / 向量库 ───────────────────────────────────────
*.db
*.sqlite
*.sqlite3
chroma/
.chroma/
faiss_index/
*.index

# ── 日志 / 运行时缓存 ─────────────────────────────────────
*.log
logs/
.cache/
tmp/
temp/

# ── IDE ──────────────────────────────────────────────────
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ── 操作系统 ─────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
