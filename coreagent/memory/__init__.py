"""记忆系统（#0010）：项目指令文件 + 会话摘要存档/恢复 + 自动笔记。

三块职责各自成模块，调用方按需从子模块导入（包级保持轻量、不在此做重导出，避免 provider
等底层模块 import 时触发重链路）：

  constants        —— 固定文案 / 阈值 / 层级与类别枚举（spec 砍掉的具体值集中落此）
  paths            —— 项目哈希 / 会话 ID / 目录布局 / 过期清理
  instructions     —— 四层项目指令加载 + @include 递归展开（限深度、防越界）
  notes            —— 四类自动笔记的存储 / 索引 / 每轮 LLM 更新
  session_summary  —— per-session summary.md 的更新（复用 #0009 摘要器）与启动恢复

边界：注入复用 #0006、摘要复用 #0009、会话历史与 #0004 并存（不重构其契约）。
"""
